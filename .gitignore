*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
/app/release

# CI 打包时会自动下载 Termux bootstrap（体积较大，不入库）
/app/src/main/assets/bootstrap-*.zip

# ★ 签名密钥与配置——泄露 = 别人能冒充你发升级包，绝对不要入库！
keystore.properties
*.jks
*.keystore
/keystore/
/release.jks

# 解密/编辑用的临时工作目录（含明文 index.html，绝不入库）
/_work/

# 本地编译实验残留（避免误入库）
/_build/
/repo.xml
